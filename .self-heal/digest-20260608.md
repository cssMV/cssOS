# cssOS 每日自愈简报 — 2026-06-08

> 自动例程 `cssos-daily-self-heal-digest`。只读 + 本地起草,**未做任何生产部署**。

## 状态:⚠️ 取数失败 — 无法连接生产服务器

今日无法获取错误 digest,因为生产主机不可达。

### 失败详情

| 步骤 | 结果 |
|---|---|
| SSH `api-vm` (34.122.162.77:22) 第 1 次 | ❌ `Operation timed out`(exit 255) |
| SSH `api-vm` 第 2 次(ConnectTimeout=20) | ❌ `Operation timed out`(exit 255) |
| ICMP ping 34.122.162.77 ×2 | ❌ 100% packet loss(全部超时) |

SSH 配置正常解析(user=jing, hostname=34.122.162.77, port=22),即主机名/账号无误;
是**网络层**不通 —— SSH 与 ICMP 双双超时,说明 VM 关机、被防火墙/安全组屏蔽、
或本机当前网络无法路由到该 IP。已按例程规则**重试 2 次后停止**,不再继续。

### 影响

- 无法读取 `GET /api/admin/error-digest`,本轮**无错误分诊、无修复草案**产出。
- 这不代表「今日无错误」—— 仅代表本轮**未能取数**。

### 建议(交人工)

1. 确认 GCE 实例 `api-vm` 是否在线 / 被重启 / 被抢占(spot 实例?)。
2. 确认本机网络能否出站到该 IP(VPN/防火墙/出口变更?)。
3. 主机恢复后,可手动重跑取数命令验证:
   ```
   ssh api-vm 'T=$(sudo grep -oE "CSSOS_ADMIN_TOKEN=.*" /etc/cssos.env | head -1 | cut -d= -f2- | tr -d "\042"); curl -s -H "x-admin-token: $T" http://localhost:3000/api/admin/error-digest'
   ```
   端点 200 返回后,重跑本例程即可正常分诊。

## 一句话总结

**今日 0 个 bug 被分诊 —— 因生产主机(34.122.162.77)SSH + ping 双双不可达,取数失败;非「无错误」。无高风险项需处理,但请人工确认 api-vm 实例与本机出站网络状态后重跑。未做任何生产部署。**
