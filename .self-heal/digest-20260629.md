# cssOS 每日自愈简报 — 2026-06-29

> 自动例程产出。**全程只读 + 本地起草,零生产写入。** 修复需 Jing 审核后手动上线。

## ① Digest 摘要

域聚合(by_domain,按命中):

| domain | hits | kinds |
|---|---|---|
| ui | 3793 | 325 |
| network | 1280 | 297 |
| generation | 605 | 22 |
| ops | 266 | 1 |
| auth | 218 | 54 |
| payment | 28 | 7 |
| data | 4 | 3 |

Top 指纹(按 count):

| # | count | code | message(截断) | last_seen | 分诊 |
|---|---|---|---|---|---|
| 1 | 602 | whisperx_worker_down | whisperX worker (10.128.0.5:7892) 不可达 ~9min | 06-27 | 🟡 基础设施(已自愈) |
| 2 | 302 | auth_required | 游客可浏览,生成需登录 | 06-17 | ⚪ 噪音(预期门禁) |
| 3 | 266 | static_perm_500 | static UNREADABLE 600→nginx500,auto-chmod 需 root | 06-28 | 🟡 运维/部署(脚本可硬化) |
| 4 | 198 | api_404 | `/api/person-mv/mvs/#/comments` | **06-29(今日活跃)** | 🔴 **真实代码 bug** |
| 5 | 180 | i18n_leak | "中文" @ `#foryou-market-lang-chips>button` | 06-16 | ⚪ 噪音(语言名 endonym) |
| 6 | 175 | i18n_leak | "日本語" @ 同上 | 06-16 | ⚪ 噪音(同上) |
| 7 | 173 | js_reject | TypeError: Load failed (VIDEO) | 06-29 | ⚪ 噪音(网络抖动/媒体加载) |
| — | 152~42 | mem_growth / oom_* | DOM 增长 / OOM 自愈重载(logo+watch panel) | 06-15~29 | 🟠 已知顽疾(探针在跟) |
| — | 79/56 | i18n_leak | agent 建议 "🎬 Trilogy template 《朋友兄弟》/《长相思》" | 06-28 | 🟠 真实但需确认(内容 vs UI) |
| — | 46 | i18n_leak | "中华佛教神话" @ person-mv-civ-select option | 06-16 | 🟠 真实(civDisplayName 缺映射兜底) |

趋势:`ui` 域仍是大头(3793),其中 OOM/mem_growth 系列(logo-panel + watch-panel)是长期累积顽疾,已有探针(W794/W1446q 硬顶)在跟,非本轮新增。今日新鲜活跃的是 **api_404 `#`** 与零散 `js_reject: Load failed`。

---

## ② 真实 bug — 根因 + 建议 diff

### 🔴 BUG-1(本轮唯一建议立即修):占位 workId `"#"` 泄漏进评论接口 → 404

- **指纹** `1da736d37e60247fa8a552fc` · count 198 · **今日 12:38 仍在发生** · sample_action `▶Watch MV`
- **现象**:`GET /api/person-mv/mvs/%23/comments` 404(`%23` = `#`)。
- **根因**:[public/app.person-mv-comments-share.js:466](public/app.person-mv-comments-share.js#L466) 的 `open(mvId)` 仅做 `if (!mvId) return;`。占位/未就绪 work 的 id 是字符串 `"#"`(真值),绕过守卫 → `currentMvId = "#"` → `fetchComments("#")` 命中 404。`share(mvId)`(:543)同样仅 `!mvId` 守卫。
- **吻合铁律**:记忆 `cross_file_symbol_defensive_ref.md` 已立规「占位 ID workId='#' 先拒」;本处缺该防御。
- **引导式 UX**:不静默 `return`,给一条带语义的 toast(走 `tr()`,英文为源,绝不硬编码中文)。
- **风险**:**低**。纯前端;只收紧入口守卫,不改接口/不改播放/不碰招牌组件。
- **已 `node -c` 验证**(scratch 副本,未落真树):语法 OK。

建议 diff(`public/app.person-mv-comments-share.js`,`open()` 之前新增 helper,并改两处守卫):

```js
  function isValidMvId(id) {
    var s = String(id == null ? "" : id).trim();
    return s !== "" && s !== "#" && s !== "undefined" && s !== "null";
  }

  async function open(mvId) {
-    if (!mvId) return;
+    if (!isValidMvId(mvId)) { toast(tr("This work is still loading — try again in a moment.", "")); return; }
     injectStyle();
     ...

  async function share(mvId) {
-    if (!mvId) return;
+    if (!isValidMvId(mvId)) { toast(tr("This work is still loading — try again in a moment.", "")); return; }
```

> `tr/toast` 均在本文件内定义(:10 / :86),无新依赖。上线照「工作三部曲」:前端改 → 重打包 + bump sw → commit。

---

## ③ 其余真实项(根因 + 建议,本轮不动手,待 Jing 定夺)

### 🟡 INFRA-1:whisperX worker 短时不可达(已自愈)
- count 602,但 last_seen 06-27;今日 06-29 现场探针 `http://10.128.0.5:7892/health → 200`,**已恢复**。属一次 ~9min 的 VM/服务窗口性中断(对齐服务一停,全平台新字幕断供)。
- 记忆 `whisperx_worker_on_dedicated_vm.md` / `whisperx_align_hang_blocks_all_subtitles.md` 已记此模式 + backlog「待加定时 ping 自愈」。
- **建议(非本轮)**:落地 worker 健康 watchdog(定时 ping → 失败自动 restart 进程 / 云控制台开机告警),把「人肉发现」变成「自愈」。风险中,涉服务端,需单独 wave。

### 🟡 OPS-1:静态资源权限 500(static_perm_500,266)
- 部署后 `public/` 资源 600 权限 → nginx 500 → nosniff 拒执行 → UI 冻结;auto-chmod 因需 root 失败。message 自带补救:`ssh api-vm 'sudo chmod ...'`。
- 记忆 `deploy_public_assets_separately.md` 已记此冻结 playbook。
- **建议(非本轮)**:在部署脚本 rsync public/ 之后追加一步 `sudo chmod -R a+r`(或 644 文件 / 755 目录),消除手工补救。**注:我被禁止改部署脚本/碰服务器,仅记录建议。** 风险低但属部署链,需 Jing 手动改 `scripts/deploy-express.sh`。

### 🟠 I18N-1:civ-select 兜底回退原始中文(46)
- [public/app.person-mv-panel.js:1937](public/app.person-mv-panel.js#L1937) 已正确路由 `globalThis.civDisplayName(c, locale)`,但 "中华佛教神话" 在 `civDisplayName`(`public/app.civ-language-map.js`)无映射条目 → 函数回退返回原始中文 `c`。
- **建议(非本轮)**:① 给 `civ-language-map` 补齐缺失文明的英文显示名;或 ② 让 `civDisplayName` 对未知 civ 走 `tr(c)` 兜底(英文/懒翻译),而非原样吐中文。风险中(动共享映射,影响多面板),需单独 wave。

### 🟠 I18N-2:agent 建议模板含中文标题(79 + 56)
- "🎬 Trilogy template 《朋友兄弟》"、"🎴 Trilogy template 《长相思》(Li Yu)" 等出现在 `#cssos-agent-suggestions`。这些是**作品/模板专名**(《朋友兄弟》《长相思》),介于「UI 字符串(须 tr)」与「内容专名(可保留)」之间。
- **建议(非本轮)**:需 Jing 拍板这些模板名是内容还是 UI。若 UI,则模板标签前缀 "Trilogy template" 走 `tr()`,专名保留。**不擅自改**(可能误伤有意内容)。

### ⚪ 噪音清单(已跳过,不计可修 bug)
- `auth_required`(302):游客生成门禁,**预期行为**,非 bug。
- `i18n_leak "中文"/"日本語" @ foryou-market-lang-chips`(180/175):语言选择 chip 显示各语言**自称(endonym)**,本就是语言选择器的标准做法;i18n-leak 检测器误报。**建议**给探针加 lang-chip 白名单(可选,极低优先级)。
- `js_reject: TypeError: Load failed`(173)、各 `api_neterr /api/me /api/mv/languages /api/dm/unread-count …`:第三方/客户端网络瞬时失败,非可修代码 bug。
- `api_409 /api/mv/lyrics`(87):并发/幂等冲突,瞬时,非新增。
- `mem_growth / oom_precursor / oom_selfheal`(152/120/87/…):长期 DOM 增长顽疾,已有 W794 自愈重载 + W1446q 爆字层硬顶在兜底;真泄漏断根属既有 backlog,非本轮新增,不重复起草。

---

## ④ 一句话总结

今日发现 **1 个建议立即修的真实代码 bug**(占位 `workId="#"` 泄漏进 person-mv 评论接口 → 404,今日仍活跃,已备低风险前端 diff + node -c 通过);**无「高风险需立即人工处理」项**(whisperX 已自愈、static_perm 属部署链)。另有 2 个 i18n 真实项 + 1 个 watchdog/部署脚本硬化建议,均列为单独 wave,待 Jing 审核后手动上线。
